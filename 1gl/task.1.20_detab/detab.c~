#include <stdio.h>
#include <stdlib.h>

#define			DEFTABSIZE 		4

int			main(int argc, const char **argv){
	int			tabsize = DEFTABSIZE;
	
	if (argc > 1)
		tabsize = atoi(argv[1]);

	TODO:

	return 0;
}
